# App metadata for Fastlane (optional for OTA lane).
# Used if you add other lanes (e.g. build, screenshots).
app_identifier("org.reactjs.native.example.NitroUpdateExample")
apple_id(ENV["FASTLANE_APPLE_ID"])
team_id(ENV["FASTLANE_TEAM_ID"])
