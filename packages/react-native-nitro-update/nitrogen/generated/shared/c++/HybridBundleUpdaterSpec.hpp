///
/// HybridBundleUpdaterSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif



#include <NitroModules/Promise.hpp>
#include <string>
#include <NitroModules/Null.hpp>
#include <variant>
#include <optional>

namespace margelo::nitro::nitroupdate {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `BundleUpdater`
   * Inherit this class to create instances of `HybridBundleUpdaterSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridBundleUpdater: public HybridBundleUpdaterSpec {
   * public:
   *   HybridBundleUpdater(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridBundleUpdaterSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridBundleUpdaterSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridBundleUpdaterSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual std::shared_ptr<Promise<bool>> checkForUpdate(const std::string& versionCheckUrl) = 0;
      virtual std::shared_ptr<Promise<void>> downloadUpdate(const std::string& downloadUrl, const std::optional<std::variant<nitro::NullType, std::string>>& bundlePathInZip, const std::optional<std::variant<nitro::NullType, std::string>>& checksum) = 0;
      virtual std::variant<nitro::NullType, std::string> getStoredVersion() = 0;
      virtual std::variant<nitro::NullType, std::string> getStoredBundlePath() = 0;
      virtual void reloadApp() = 0;
      virtual void confirmBundle() = 0;
      virtual std::shared_ptr<Promise<bool>> rollback() = 0;
      virtual std::shared_ptr<Promise<void>> markBundleBad(const std::string& reason) = 0;
      virtual std::shared_ptr<Promise<std::string>> getBlacklist() = 0;
      virtual std::shared_ptr<Promise<std::string>> getRollbackHistory() = 0;
      virtual void scheduleBackgroundCheck(const std::string& versionCheckUrl, const std::optional<std::variant<nitro::NullType, std::string>>& downloadUrl, double intervalSeconds) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "BundleUpdater";
  };

} // namespace margelo::nitro::nitroupdate
